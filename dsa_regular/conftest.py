import pytest

# from dsa_regular.users.models import User
# from dsa_regular.users.tests.factories import UserFactory


# @pytest.fixture(autouse=True)
# def media_storage(settings, tmpdir):
#     settings.MEDIA_ROOT = tmpdir.strpath


# @pytest.fixture
# def user(db) -> User:
#     return UserFactory()
